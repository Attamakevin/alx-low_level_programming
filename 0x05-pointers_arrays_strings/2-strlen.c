include "main.h"
/**
* _strlen - funtion that calculate the lentgth of a string
* @s : input
* Return:  length.
*/
int _strlen(char *s)
{
	int lenth = 0

	while (s * != '\0')
	*s++;
	lenth++;
	return (lenth);
}
