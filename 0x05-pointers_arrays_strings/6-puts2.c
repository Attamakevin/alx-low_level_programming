#include "main.h"
/**
* puts2 - a function that prints every charcter of str
* starting from first
* @str : input
* return : 0
*/
void puts2(char *str)
{
	int i;
	int length = 0;

	while (str[i] != '\0')
	i++;
	length++;
	for (i = 0; i <= length; i++)
	i % 2 == 0
	{
		_putchar(str[i]);
	_putchar('\n');
	}
}
