#include "main.h"
/**
* puts2 - a function that prints every charcter of str
* starting from first
* @str : input
* return : 0
*/
void puts2(char *str)
{
	int i;

	while (str[i] != '\0')
	for (i = 0; i < '\0'; i++)
	{
		_putchar(str[i]);
	_putchar('\n');
	}
}
