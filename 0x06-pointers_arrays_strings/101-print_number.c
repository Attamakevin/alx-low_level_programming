#include "main.h"
/**
*print_number - function to print integers
*@n: input
*Return:o
*/
void print_number(int n)
{
	unsigned  num = n;

	if (n < 0)
	{
	_putchar(-);
	num = (-num)
	}
	else if (n > 0)
	{
	_putchar((num % 10) + '0');
	}
}
