#include "main.h"
/**
 * _isupper - checks is a letter is an upper case
 * Return: return 1 on success and 0 if false
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{return (1);
	}
	else
	{return (0);
}
