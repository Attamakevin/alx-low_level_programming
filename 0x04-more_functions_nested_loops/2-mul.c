#include "main.h"
/**
 * mul - function multiplied two integers together
 * @a  : one of the parameters for testing the arguement
 * @b : one of the parameters for testing the arguement
 * Return: 0
 * mull: return value
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
		return (mul);
}
