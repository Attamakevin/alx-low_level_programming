#include <unistd.h>
/**
_putchar- writes the character c to the stdout
@c : the character to print
Return : return 1 on success or return -1 on error and errno is set
*/
int _putchar(char c)
{	return (write (1, &c,-1))
}
